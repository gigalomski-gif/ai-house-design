<!DOCTYPE html>
<html lang="ka">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI ?????? ???????? ??????????</title>

<style>
body {
    font-family: Arial, sans-serif;
    background: #f4f6f9;
    text-align: center;
    padding: 30px;
}

.container {
    max-width: 800px;
    margin: auto;
    background: white;
    padding: 25px;
    border-radius: 15px;
    box-shadow: 0 5px 20px rgba(0,0,0,0.1);
}

input[type="file"] {
    margin: 15px 0;
}

button {
    background: #2c7be5;
    color: white;
    padding: 12px 25px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 16px;
}

button:hover {
    background: #1a5fd1;
}

img {
    max-width: 100%;
    margin-top: 20px;
    border-radius: 10px;
}

#result {
    margin-top: 20px;
    text-align: left;
    background: #f1f3f6;
    padding: 15px;
    border-radius: 10px;
}
</style>
</head>

<body>

<div class="container">
    <h2>AI ?????? ???????? ????????????</h2>

    <input type="file" id="imageInput" accept="image/*">
    <br>
    <button onclick="analyzeImage()">???????? ??????????</button>

    <div>
        <img id="preview" />
    </div>

    <div id="result"></div>
</div>

<script>
const preview = document.getElementById('preview');
const imageInput = document.getElementById('imageInput');
const resultDiv = document.getElementById('result');

imageInput.addEventListener('change', function() {
    const file = this.files[0];
    const reader = new FileReader();
    reader.onload = function(e) {
        preview.src = e.target.result;
    }
    reader.readAsDataURL(file);
});

async function analyzeImage() {
    if (!imageInput.files.length) {
        alert("????? ??????? ????");
        return;
    }

    resultDiv.innerHTML = "???????????? ???????...";

    // ?? ?? ?????? ???? OpenAI API KEY-?
    const API_KEY = "PASTE_YOUR_OPENAI_API_KEY_HERE";

    const base64Image = preview.src.split(',')[1];

    const response = await fetch("https://api.openai.com/v1/responses", {
        method: "POST",
        headers: {
            "Content-Type": "application/json",
            "Authorization": `Bearer ${API_KEY}`
        },
        body: JSON.stringify({
            model: "gpt-4.1",
            input: [{
                role: "user",
                content: [
                    { type: "input_text", text: "Analyze this house image and suggest optimal staircase placement, window positioning, and layout improvements." },
                    {
                        type: "input_image",
                        image_base64: base64Image
                    }
                ]
            }]
        })
    });

    const data = await response.json();
    const aiText = data.output[0].content[0].text;

    resultDiv.innerHTML = "<strong>AI ????????????:</strong><br><br>" + aiText;
}
</script>

</body>
</html>
